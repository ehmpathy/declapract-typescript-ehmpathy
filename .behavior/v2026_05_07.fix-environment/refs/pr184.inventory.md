# PR #184 File Inventory

5 additions, 69 deletions. 4 files changed.

---

## APPLICABLE FILES

### 1. package.json
- **change**: 1 deletion, 1 addition
- **applicable**: YES
- **pattern**: deploy:prune needs COMMIT env var for serverless.yml resolution
```diff
- "deploy:prune": "SLS_STAGE=$(if [ \"$ACCESS\" = 'prep' ]; then echo 'dev'; else echo \"$ACCESS\"; fi) && npx sls prune -n 7 --stage $SLS_STAGE",
+ "deploy:prune": "SLS_STAGE=$(if [ \"$ACCESS\" = 'prep' ]; then echo 'dev'; else echo \"$ACCESS\"; fi) && export COMMIT=\"prune@$(git rev-parse --short HEAD)\" && npx sls prune -n 7 --stage $SLS_STAGE",
```

**key insight**: serverless.yml requires `COMMIT` env var (`${env:COMMIT}`). both `deploy:release` and `deploy:prune` must export it.

### 2. provision/schema/connection.config.js
- **change**: 3 deletions
- **applicable**: YES — CORRECTION to PR #182
- **pattern**: REMOVED CONFIG check (was added in PR #182, deemed unnecessary)
```diff
- if (!process.env.CONFIG) {
-   throw new Error('CONFIG required. set CONFIG=test|prep|prod');
- }
```

**key insight**: the explicit CONFIG check is redundant — sdk-config will fail with a clear error if config file not found. the check was removed to simplify.

---

## NOT APPLICABLE FILES

### 3. .agent/.cache/repo=.this/role=any/skills/show.cloudwatch.logs/.gitignore
- **change**: 4 additions (new file)
- **applicable**: NO
- **reason**: repo-specific cache gitignore for cloudwatch logs skill

### 4. .behavior/v2026_05_07.fix-prep-pgschemas/5.1.execution.from_vision.yield.md
- **change**: 65 deletions (deleted file)
- **applicable**: NO
- **reason**: behavior route yield file cleanup

---

## SUMMARY

### APPLICABLE (2 files)

| file | pattern |
|------|---------|
| `package.json` | `deploy:prune` must export `COMMIT` (absent in PR #182) |
| `provision/schema/connection.config.js` | REMOVE explicit CONFIG check (redundant) |

### NOT APPLICABLE (2 files)

| file | reason |
|------|--------|
| `.agent/.cache/.../show.cloudwatch.logs/.gitignore` | repo-specific cache |
| `.behavior/.../*.yield.md` | behavior route cleanup |

---

## CORRECTIONS TO VISION DOCUMENT

PR #184 reveals one correction to PR #182 patterns:

1. **provision/schema/connection.config.js**: the CONFIG env var check should NOT be in best-practice. it was added in PR #182 but removed in PR #184 as redundant.

The vision document currently shows:
```js
if (!process.env.CONFIG) {
  throw new Error('CONFIG required. set CONFIG=test|prep|prod');
}
```

This should be removed from the vision — sdk-config handles absent config files with clear errors already.
