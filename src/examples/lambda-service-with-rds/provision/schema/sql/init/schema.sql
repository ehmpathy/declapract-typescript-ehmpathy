/**
  NOTE:
    - this must be run manually in live env (not done with schema-control, so that schema-control can operate on this schema)
    - this is done automatically in integration-test-db env
*/
CREATE SCHEMA @declapract{variable.databaseName};
