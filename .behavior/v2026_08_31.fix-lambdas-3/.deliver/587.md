delivered on branch vlad/fix-lambdas-3.

shipped a `boot.yml` TEMPLATE via the rhachet practice at
`src/practices/rhachet/best-practice/.agent/repo=.this/role=any/boot.yml`, so every consumer inherits glossary discoverability on `declapract apply`.

## the shape — a GLOB, not an enumeration (the ticket's preferred form)

```yaml
always:
  briefs:
    ref:
      - briefs/domain.terms/.readme.md
      - briefs/domain.terms/term=*._.choice._.md
```

i confirmed boot.yml supports globs by a read of the installed rhachet source
(`dist/domain.operations/boot/computeBootPlan.js` + `filterBootResourcesByGlob.js`): `always.briefs.ref` entries are patterns matched via fast-glob against the discovered brief set, so the glob is:
- **per-consumer correct** — it matches each consumer's OWN `domain.terms/` tree, never this repo's specific term list (the anti-pattern the ticket's "per-consumer glossary" note warns against).
- **drift-proof** — a new term auto-matches, so the boot list cannot fall behind the glossary. this obviates the ticket's "add a ref line per new term" checklist note (that was only needed for the enumerated form).
- **say-level only** — `term=*._.choice._.md`'s `._.md` tail excludes `._.choice.reason.md` and `._.choice.example=*.md`, so the deep reason files stay on-demand (their say file points to them), exactly as the ticket asks.

## clamp

`src/practices/rhachet/boot-refs-glossary.declapract.test.ts` (3 tests) — at the PRACTICE ROOT, not under best-practice/ (a `.declapract.test.ts` under best-practice/ would be copied to consumers). asserts: the glob ref is present, the readme ref is present, and NO concrete `term=<slug>` is hardcoded (the per-consumer-incorrect enumeration). teeth confirmed: a swap of the glob for an enumerated `term=convergence._.choice._.md` reddens 2 (glob-present + no-hardcoded-term). GREEN with the glob; typecheck clean.

## bounds / not-done

- **this repo's own instance not hand-created (deliberate):** this repo consumes its own rhachet practice, so it too should carry `.agent/repo=.this/role=any/boot.yml`. i did NOT hand-place it — per rule.require.entool-requests-as-practices the practice declares it and `declapract apply` (or the self-check) drives it into this repo. a hand-edit would be the adhoc consumer edit the wish forbids. NB: until applied, this repo's role=any still loads all domain.terms as `say` (full content); the boot.yml moves them to `ref` (path-only) — a token win the consumer gets on apply.
- no Role.build() change: this is a declapract template for consumer trees, picked up by rhachet's boot hook via the `.agent/repo=.this/role=any/` path convention (the same convention that boots role=any today) — not a role definition in this repo's source.
