# verification checklist

## behavior coverage (from wish/vision)

| behavior | implementation | test coverage | status |
|----------|----------------|---------------|--------|
| production-on-main environment | provision/github/declastruct.resources.ts:124-132 | declarative config, no behavior to test | ✓ |
| production-on-main in template | src/practices/provision-github/best-practice/.../resources.ts:124-132 | declarative config, no behavior to test | ✓ |
| production-on-else environment | provision/github/declastruct.resources.ts:135-143 | declarative config, no behavior to test | ✓ |
| production-on-else in template | src/practices/provision-github/best-practice/.../resources.ts:136-144 | declarative config, no behavior to test | ✓ |
| declastruct upgrade 1.7.0 → 1.9.1 | package.json | types pass | ✓ |
| declastruct-github upgrade 1.0.7 → 1.4.0 | package.json | types pass | ✓ |
| cicd-service declastruct 1.7.3 → 1.9.1 | src/practices/cicd-service/best-practice/package.json | declapract compile | ✓ |
| provision-github declastruct 1.7.3 → 1.9.1 | src/practices/provision-github/best-practice/package.json | declapract compile | ✓ |
| provision-github declastruct-github 1.3.5 → 1.4.0 | src/practices/provision-github/best-practice/package.json | declapract compile | ✓ |

## zero skips verified

- [x] no .skip() or .only() found
- [x] no silent credential bypasses
- [x] no prior failures carried forward

grep for `.skip\(|\.only\(|it\.skip|describe\.skip|test\.skip` in `**/*.test.ts` returned: No matches found

## snapshot coverage for contract outputs

this is a declapract practices repo — it defines declarative configurations (best-practice templates), not executable contracts (cli, api, sdk). the "contracts" here are:

| artifact type | what it is | test strategy |
|---------------|------------|---------------|
| best-practice package.json | version requirements | declapract compile validates |
| best-practice resources.ts | declarative domain objects | types check validates |
| local resources.ts | declarative domain objects | types check validates |

no cli commands, api endpoints, or sdk methods were added. snapshot coverage is not applicable.

## snapshot change rationalization

no `.snap` files were changed or created. command: `git diff HEAD -- '*.snap'` returned empty.

## tests executed — with proof

| test suite | command run | result | proof (exit code + summary) |
|------------|-------------|--------|----------------------------|
| types | `rhx git.repo.test --what types` | ✓ | exit 0, passed (2s) |
| format | `rhx git.repo.test --what format` | ✓ | exit 0, passed (2s) |
| lint | `rhx git.repo.test --what lint` | ✓ | exit 0, passed (4s) |
| unit | `rhx git.repo.test --what unit --thorough` | ✓ | exit 0, 291 tests passed, 0 failed, 0 skipped (8s) |
| integration | `rhx git.repo.test --what integration --thorough` | ✓ | exit 0, 5 tests passed, 0 failed, 0 skipped (5s) |
| acceptance | `rhx git.repo.test --what acceptance --thorough` | ✓ | exit 0, 0 tests (no acceptance tests in this repo) |

## contract output snapshot exhaustiveness

not applicable — this is a declarative practices repo with no user-faced cli/api/sdk contracts.

## blockers

none.

## notes

- this pr adds declarative github environment configurations
- no executable code was added, only domain object declarations
- test coverage for declarative config is via type check and declapract compile
- the patterns follow extant structure in this practices repo (no tests for declarative config)
