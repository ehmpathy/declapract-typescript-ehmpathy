wish =

we want to upgrade to use `sdk-environment` instead of our adhoc environment.ts parser

checkout how /home/vlad/git/ahbode/svc-jobs 's has done it, via git.repo.get

---

btw, ❯ btw, we should look for opportunities to eliminate teh STAGE env var, as well 

❯ we should prefer ACCESS=prep|prod instead of STAGE=dev|prod 


taht may impact cicd and acceptance tests

---

however, we cant yet cutover the config location from config/dev.json (that is a requirement)

and we cant yet cutover the serverless.yml service "stage" from dev, since the lambdas are already deployed as `svc-xys-dev`

so we'll still want a couple thin translators from `prep` -> `dev` in thsoe boundaries

we'll push that through later
