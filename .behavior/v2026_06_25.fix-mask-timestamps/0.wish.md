wish =

we had a husky rule which blocked any attempts to commit files that have timestamps in them (T?HH:MM:SS{timezone}) 

unless its a .ts or a .sh file

and halted with the constraint error that says "timestamps are forbidden in snapshots. mask them to prevent permadrift"


