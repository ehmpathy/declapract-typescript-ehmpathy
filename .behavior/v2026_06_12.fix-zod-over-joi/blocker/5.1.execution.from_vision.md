# blocker: peer review flags library conventions as violations

## current status

tests pass (37/37). implementation complete. blockers are all false positives due to:

1. **declapract library conventions** — check functions use throw/return semantics per library API
2. **extant codebase patterns** — all 50+ extant practices use plain `Error`, not `ConstraintError`
3. **missing package context** — reviewer didn't see separate `joi/` directory handles package.json

## the 5 blockers (all false positives)

| review | count | issue | why false positive |
|--------|-------|-------|-------------------|
| r1: mech-failhides | 3 | wants ConstraintError | ConstraintError doesn't exist in packages; codebase uses Error |
| r6: arch-hazards-behavior | 1 | regex may match strings | documented limitation in .note; vision says manual fixes ok |
| r7: behavior-intent-coverage | 1 | missing package.json | FALSE — `joi/` directory extant with package.json.declapract.ts |

## evidence for r7 being false

```
src/practices/runtime-type-checking/bad-practices/
├── joi/                      # <-- handles package.json (joi dep removal)
│   ├── package.json
│   ├── package.json.declapract.ts
│   └── package.json.declapract.test.ts
├── joi-imports/              # <-- handles source file transforms
│   └── src/**/*.ts.declapract.ts
```

the joi-imports bad-practice is ONLY for source file transforms. the joi bad-practice handles package.json.

## what i need

human override to mark stone as passed. the implementation is correct per extant patterns.

run: `rhx route.stone.set --stone 5.1.execution.from_vision --route .behavior/v2026_06_12.fix-zod-over-joi --as passed`
