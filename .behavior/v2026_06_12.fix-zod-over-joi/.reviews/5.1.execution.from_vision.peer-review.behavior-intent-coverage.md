🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-25T01-46-04-817Z
   ├─ review: .behavior/v2026_06_12.fix-zod-over-joi/.reviews/5.1.execution.from_vision.peer-review.behavior-intent-coverage.md
   └─ summary
      ├─ 1 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: Missing package.json handling for joi dependency removal

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.require.behavior-intent-coverage.md

**locations**:
- src/practices/runtime-type-checking/bad-practices/joi-imports/src/**/*.ts.declapract.ts

The wish and vision require removing the `joi` dependency from package.json and adding `zod`, as well as removing `@types/joi` from devDependencies. The implementation of the joi-imports bad practice only handles source file imports and schema patterns but lacks a package.json practice (unlike the simple-lambda-client and simple-lambda-handlers practices which have corresponding package.json.declapract files). This gap means `declapract plan` will not flag the joi dependency and `declapract fix` will not remove it, leaving a critical requirement unaddressed.

**snippet**:
```ts
export const check: FileCheckFunction = (contents) => {
  // match if file imports from joi
  if (contents?.includes("from 'joi'")) return;
  if (contents?.includes('from "joi"')) return;

  // no match
  throw new Error('does not import from joi');
};
```
