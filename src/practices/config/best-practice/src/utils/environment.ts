// this is a placeholder file, so that the declared acceptance test file passes typescript type checking. for this practice, we only check that the file exists
export const stage = 'placeholder' as any;
export const Stage = 'placeholder' as any;
