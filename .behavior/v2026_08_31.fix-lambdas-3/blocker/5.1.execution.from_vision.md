# blocker — 5.1.execution.from_vision

## what blocks you

the stone's 11 review lanes have converged: **9 L1 lanes approved (0 blockers)**, and after a
driver guard-edit fixed a spawn-overflow, **both L3 lanes now produce real verdicts** —
`enroll-impl-arch-defects` **approved (0 blockers, 3 nitpicks)**, `enroll-impl-behavior-intent`
raised **1 blocker + 1 nitpick**. total nitpicks = 7, at the `--allow-nitpicks 7` threshold. the
judge blocks on the single leftover blocker (`1 > 0`).

that one blocker is a **git index ↔ worktree drift** — real, verified, and its only close needs a
grant this driver does not hold:

- `AD src/practices/environments/best-practice/src/access/sdks/sdkAwsLambdaEnvAccess.ts` — the stale
  single-file helper is **added in the index but deleted in the worktree**.
- `?? …/sdkAwsLambdaEnvAccessAncient.ts`, `?? …/sdkAwsLambdaEnvAccessContemp.ts` — the tested split
  that replaced it, untracked. the integration test
  (`environments/.declapract.integration.test.ts:27-29,67-132`) reads the split by path.
- ` M …/config/getCacheDirectory.ts` (unstaged) imports `?? …/config/getProjectBucket.ts` (untracked)
  — the i055 mech-decode-friction extraction.
- `?? …/tests-service/__snapshots__/*.acceptance.test.ts.declapract.test.ts.snap` — untracked while
  its parent test is staged.

the worktree itself is self-consistent and tested (the L3 reviewer confirmed the split, the config
co-declaration, and every clamp hold "with real teeth"). the hazard is only that a **bare-index
commit** would ship the stale shape and drop the split — so the index must be aligned to the worktree
before this route is release-ready.

## what you tried

- ran all 11 lanes to convergence over 58 iterations; drove the 9 L1 lanes to 0 blockers.
- diagnosed the L3 spawn-overflow (empty stdout, exit 1, absent `😶 clone enrolled` banner, stable
  across i056+i057) as an oversized `$conversation` enroll argv, and applied the **driver-owned guard
  edit** (`rule.always.diagnose-reviewer-malfunctions`): cut the `$conversation` read from both L3
  prompts, re-grounded them on the bounded `src/` tree + wish + vision. at i058 both lanes ran clean
  (533s / 595s, banners present) — the fix held.
- spent my one permitted lever for the drift: `rhx git.stage.add <the five paths>` → returned
  `error: stage not allowed`. the stage grant is human-gated (`git.commit.uses set … --stage allow`).
- a `git commit` is forbidden here (`rule.forbid.commits-the-route-did-not-ask-for`, no stone asked).
- a revert of the worktree to the index is the wrong direction — it would drop the tested split.
- answered every L3 point in a `.taken` (i058 r010 + r011) and contemplated both.

per `rule.always.raise-a-blocker-a-taken-cannot-close`: the blocker is answered and correct, and **no
change i am permitted to make closes it** — the sole remedy (align the index to the worktree) needs
the human stage grant. per `rule.always.spend-own-levers-before-escalation` the one lever left is
human-owned, and the wall is unambiguously a grant.

## what you need

grant stage (no commit quota needed):

```
git.commit.uses set --quant 0 --push block --stage allow
```

with that, in one post-grant iteration i will: align the index to the worktree reality
(`index == worktree` — which closes the drift blocker), and land all four nitpick repairs in the same
pass — r10's serverless-readme 75GB-cap caution, r11's config.schema.ts `EQUALS`→`CONTAINS` companion,
the deployment-bucket `@declapract:review` marker, and the `getAllPracticesForUsecase.ts` comment fix
— then re-arrive. L3 re-runs clean and the judge passes (0 blockers, ≤ 7 nitpicks).
