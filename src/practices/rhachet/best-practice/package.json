{
  "devDependencies": {
    "rhachet": "@declapract{check.minVersion('1.19.1')}",
    "rhachet-roles-ehmpathy": "@declapract{check.minVersion('1.15.25')}",
    "rhachet-roles-bhrain": "@declapract{check.minVersion('0.5.7')}",
    "rhachet-roles-bhuild": "@declapract{check.minVersion('0.5.0')}"
  },
  "scripts": {
    "prepare:rhachet": "rhachet init && rhachet roles link --repo ehmpathy --role mechanic && rhachet roles link --repo bhuild --role behaver && rhachet roles link --repo bhrain --role reviewer && rhachet roles init --role mechanic && rhachet roles init --role behaver"
  }
}
