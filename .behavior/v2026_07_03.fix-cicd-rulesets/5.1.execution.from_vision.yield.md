# 5.1 execution — from vision

> wish: ehmpathy/declapract-typescript-ehmpathy#515
> vision: `.behavior/v2026_07_03.fix-cicd-rulesets/1.vision.yield.md`

add the `protect-release-tags` github repo-ruleset to the `provision-github` best-practice
so `declapract fix` propagates release-tag protection to all typescript repos.

---

## todos

- [x] add `DeclaredGithubRepoRuleset` to the import block in the best-practice template
      `src/practices/provision-github/best-practice/provision/github.repo/resources.ts`
- [x] declare `rulesetReleaseTags` (name `protect-release-tags`, target `tag`, enforcement
      `active`, rhelease app `2472031` sole bypass actor, `refs/tags/v*`, rules
      create+update+delete) and append it to the returned resource set
- [x] bump the best-practice pin
      `src/practices/provision-github/best-practice/package.json`
      `declastruct-github` `minVersion('1.5.5')` → `minVersion('1.6.0')`
- [x] apply the wisher's rename: `protect-version-tags` → `protect-release-tags`
- [x] verify: `test:types` ✓ passed
- [x] verify: `fix:format` + `fix:lint` ✓ clean (my file not among the prior nursery
      warnings; lint gates on error-level only)
- [x] verify: `build` (`declapract compile`, 911 files) ✓ compiled
- [x] verify: unit tests ✓ 31 passed / 0 failed

---

## decisions applied (per wisher)

- **rename**: ruleset named `protect-release-tags` (not `protect-version-tags`) — the wisher
  chose the clearer intent (the rule protects *release* tags).
- **app-id**: hardcoded `2472031` (org-wide rhelease app constant) — v1 default, no new
  declapract variable.

## scope dropped during vision review

- **no change to this repo's own deps**: the template `resources.ts` is not typechecked
  here (tsconfig `include` = `src/**/*.declapract.ts` only) and `declastruct-github` is not
  a direct dep — so no `1.4.0 → 1.6.0` install bump was needed. only the best-practice pin
  changed.

## follow-ups (out of scope for this wish)

- **[wisher]** confirm ship to all `provision-github` use cases (npm-package,
  lambda-service (+rds/dynamodb), app-react-native-expo) vs a subset.
- **[note]** `app-react-native-expo` has no `provision.yml`, so the ruleset is inert there
  (declared, never applied) — a condition that predates this change.
- **[migration]** on `ehmpathy/declastruct-github` itself, its live ruleset is still named
  `protect-version-tags`; conforming this default would create a second
  `protect-release-tags` beside it (name is part of the unique key). retire the old name in
  a one-time migration when convenient.

---

## files changed

| file | change |
|------|--------|
| `src/practices/provision-github/best-practice/provision/github.repo/resources.ts` | + import `DeclaredGithubRepoRuleset`; + `rulesetReleaseTags` declaration; + return element |
| `src/practices/provision-github/best-practice/package.json` | pin `declastruct-github` `minVersion('1.5.5')` → `minVersion('1.6.0')` |
