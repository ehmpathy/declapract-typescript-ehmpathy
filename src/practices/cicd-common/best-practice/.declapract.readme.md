flows:
- test on every commit
- create please-release pull requests on every commit to main

every third-party `uses:` here is pinned to a full-length commit sha with the tag it came from
(`actions/checkout@11d5960… # v4`), because the ehmpathy org sets `sha_pinning_required`. the shape
is held by `src/actionPins.declapract.integration.test.ts`, which also carries the note on how to derive a sha
for a bump.
