# 5.3.verification — make `protect-release-tags` ruleset a default best practice

> wish: ehmpathy/declapract-typescript-ehmpathy#515
> deliverable: a declapract **best-practice template** change (+ a dep pin bump).

---

## deliverable footprint

exactly two files, both uncommitted in the work tree:

| file | change |
|------|--------|
| `src/practices/provision-github/best-practice/provision/github.repo/resources.ts` | +34 lines — `rulesetReleaseTags` declaration + import of `DeclaredGithubRepoRuleset` |
| `src/practices/provision-github/best-practice/package.json` | pin bump `minVersion('1.5.5')` → `minVersion('1.6.0')` |

neither file is a test or a `.snap`. (the large `git diff main` reflects prior
behaviors accumulated on this branch, not this deliverable.)

---

## behavior coverage (with reference to the vision)

the vision is explicit that this deliverable has **no dedicated test surface**:

> "no dedicated test surface — best-practice template files aren't unit-tested
> here the way `.declapract.ts` fix functions are; correctness rides on the
> verbatim lift + the upstream declastruct-github test suite. verification will
> lean on typecheck + a conform plan-run rather than a new test."
> — 1.vision.yield.md, "what is awkward?"

reasons this is correct, not a gap:

1. **`resources.ts` is a plain template**, not typechecked here (`tsconfig.json`
   `include` is `src/**/*.declapract.ts` only) and has **no `.declapract.ts`
   companion** → conformed via default `FileCheckType.EQUALS`. there is no fix
   function to unit-test.
2. the **`package.json` pin** is guarded by `package.json.declapract.ts`
   (`FileCheckType.CONTAINS`) — a declapract check type, no bespoke test.
3. the ruleset declaration is a **verbatim lift** of the live, dogfooded
   `declastruct-github` ruleset (id 18380440); its runtime behavior is proven by
   the upstream `declastruct-github` test suite, not re-tested here.

| behavior (from vision) | verified by | status |
|------------------------|-------------|--------|
| template gains a `protect-release-tags` ruleset declaration | `declapract compile` (911 files) + peer reviews r1-r7,r9 | ✓ |
| symbol resolves for downstream (`declastruct-github >= 1.6.0`) | pin bump verified via `git diff main` | ✓ |
| declaration is valid (fields, nest shape, `.as`) | `declapract compile` (911 files, 0 errors) | ✓ |

no behavior left uncovered by its appropriate verification mechanism.

---

## zero skips verified

- [x] no `.skip()` / `.only()` / `xit` / `xdescribe` — grep across `**/*.test.ts`: **no files found**
- [x] no silent credential bypasses in the deliverable (no test code added)
- [x] no prior failures carried forward — full suites green (below)

---

## snapshot coverage / change rationalization

the deliverable is a best-practice template file + a dep pin. it produces **no
user-faced contract output** (no cli/api/sdk) and touches **no `.snap` file**.

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| — | none | — | deliverable touches zero snapshots |

- [x] every `.snap` change reviewed — deliverable introduces none
- [x] no accidental snapshot churn attributable to this change

---

## tests executed — with proof

| test suite | command run | result | proof |
|------------|-------------|--------|-------|
| types | `rhx git.repo.test --what types` | ✓ | passed (1s), exit 0 |
| format | `rhx git.repo.test --what format` | ✓ | passed (0s), exit 0 |
| lint | `rhx git.repo.test --what lint` | ✓ | passed (3s), exit 0 |
| unit | `rhx git.repo.test --what unit --mode apply --thorough` | ✓ | 73 suites, **419 passed, 0 failed, 0 skipped** |
| integration | `rhx git.repo.test --what integration --mode apply --thorough` | ✓ | 3 suites, **19 passed, 0 failed, 0 skipped** |
| compile | `npm run build:compile` (`declapract compile`) | ✓ | **911 files compiled, 0 errors** |
| acceptance | — | n/a | **no `.acceptance.test.ts` files exist** in repo (`--passWithNoTests`) |

- [x] every test command run (not assumed)
- [x] every output observed
- [x] exit codes verified (all pass)
- [x] no tests skipped, mocked, or faked

---

## contract output snapshot exhaustiveness

not applicable — the deliverable is a declapract best-practice template, not a
user-faced contract (no cli/api/sdk surface). the "contract" here is the
conformed `resources.ts`, validated by `declapract compile`.

---

## blockers

- none.

note: the `5.1.execution.from_vision` stone carried one peer-review false
positive (r8 behavior-intent-coverage claimed an absent pin bump the reviewer's
`**/*.{ts,sh}` scope could not see in `package.json`); the pin bump is present,
confirmed via `git diff main`, and the human overruled the stone (passage =
overruled). that does not carry into verification — the requirement is met.
