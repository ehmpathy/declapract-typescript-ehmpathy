[wish] = use `database.role.cicd` and `database.role.crud` instead of `database.admin`, `database.service`


also, name the role.usernames as
- `svc_${project}_cicd`
- `svc_${project}_crud`


rather than `chatdb_cicd` and `svc_chat_user`
