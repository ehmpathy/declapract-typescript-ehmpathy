// a placeholder declastruct wish so `provision/aws/resources.ts` exists on disk.
// .why = the second callee-on-disk rds signal. a genuine rds consumer ships this
//        aws-provision wish; its presence marks the repo rds even when a narrow
//        `--practice cicd-service` apply has darkened `context.projectPractices`.
export const getResources = async (): Promise<[]> => [];
