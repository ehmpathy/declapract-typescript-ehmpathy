wish =

checkout ehmpathy/rhachet's pr #344

they just shipped out the ability to use the `rhx keyrack firewall` operation to remove the adhoc secrets parsing that we have to do

just pass in toJSON(secrets) and it does the rest

this is way more maintainable and way less complex, since it offloads the work of key filters and key sanitization

---

lets upgrade to that pattern!

