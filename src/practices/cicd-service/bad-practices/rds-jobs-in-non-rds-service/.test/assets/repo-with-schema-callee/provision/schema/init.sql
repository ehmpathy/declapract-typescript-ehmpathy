-- a placeholder schema file so `provision/schema` exists on disk.
-- .why = this fixture root marks a genuine rds consumer via a callee-on-disk, the
--        signal that survives a narrow `--practice cicd-service` apply (which darkens
--        `context.projectPractices`). the scope-robust clamp points its root here.
select 1;
