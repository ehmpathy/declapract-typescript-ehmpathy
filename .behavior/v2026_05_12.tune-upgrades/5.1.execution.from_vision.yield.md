# execution: tune-upgrades

## tasks

### 1. fix deploy.database.sh

- [x] change `databaseTunnelHost.dev` → `databaseTunnelHost.prep` at line 57

### 2. terraform directory rename

- [x] rename `environments/dev/` → `environments/prep/`
- [x] keep internal `environment = "dev"` (infrastructure stays named `dev`, application uses ACCESS=prep)
- [x] update variable references: `awsAccountId.dev` → `awsAccountId.prep`

### 3. bad-practice for terraform directory migration

- [x] create `old-dev-env-dir` bad-practice to auto-move `environments/dev/` → `environments/prep/`
- [x] update `test/main.tf`: `awsAccountId.dev` → `awsAccountId.prep`

### 4. add documentation

- [x] document `databaseTunnelHost` in persist-with-rds practice readme
- [x] document `reviewers.users` in provision-github practice readme

---

## progress log

all tasks completed.

