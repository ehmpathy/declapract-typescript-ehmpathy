since devdb scales to 0 when idle, before gh actions run integration tests, they should always wait for the livedb:dev to be accessible spinup first

i.e., be able to "select 1"

that way we dont get false-negative test errors

since devdb can take up to 15sec to scale back up
