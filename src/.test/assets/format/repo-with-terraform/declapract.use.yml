# .note = declarations dir is symlinked by the test to src/practices/format/.test/assets/declarations
# .why  = a consumer on a terraform usecase, used to prove the format practice's package.json
#         `contents` fn chains the terraform formatter into fix:format / test:format when a
#         terraform practice is in play.
declarations: 'declarations/declapract.declare.yml'
useCase: with-terraform
# .note = format + terraform-common reference NO @declapract{variable.*}, so no variables block
#         is owed here (grep both best-practice dirs to confirm).
