# blueprint: slowtest reporter best practice

## summary

add slowtest reporter to jest.integration.config.ts best practice, add .slowtest/integration.report.json to .gitignore best practice, bump test-fns minVersion to 1.15.7.

---

## filediff tree

```
src/practices/
├── tests/best-practice/
│   ├── [~] jest.integration.config.ts     # add slowtest reporter to reporters array
│   └── [~] package.json                   # bump test-fns minVersion to 1.15.7
└── git/best-practice/
    └── [~] .gitignore.declapract.ts       # add .slowtest/integration.report.json to ignoresSortable
```

---

## codepath tree

```
src/practices/tests/best-practice/jest.integration.config.ts
├── [○] config declaration
├── [~] reporters array
│   ├── [○] default reporter (summaryThreshold: 0)
│   └── [+] slowtest reporter
│       ├── [+] slow: '10s'
│       └── [+] output: '.slowtest/integration.report.json'
└── [○] rest of config

src/practices/git/best-practice/.gitignore.declapract.ts
├── [~] ignoresSortable array
│   └── [+] '.slowtest/integration.report.json'
├── [○] ignoresOrdered array
├── [○] defineExpectedContents function
├── [○] check function
└── [○] fix function

src/practices/tests/best-practice/package.json
└── [~] devDependencies
    └── [~] test-fns minVersion: '1.7.2' → '1.15.7'
```

---

## test coverage

### unit tests

no new unit tests required — extant `.gitignore.declapract.test.ts` validates the sortable ignores mechanism.

### integration tests

declapract integration tests (via `npm run test:integration`) validate that practices apply correctly.

### acceptance tests

declapract acceptance tests (via `npm run test:acceptance:locally`) validate end-to-end behavior.

### dogfood validation

this repo dogfoods the slowtest reporter in `jest.integration.config.ts` — run `npm run test:integration` to see the report.

---

## implementation notes

1. **jest.integration.config.ts**: add slowtest reporter to reporters array, keep the default reporter.

2. **.gitignore.declapract.ts**: add entry to `ignoresSortable` array — the dedupe/sort mechanism handles order.

3. **package.json**: bump test-fns minVersion to 1.15.7 (latest stable).

all three changes follow extant patterns with minimal surface area.
