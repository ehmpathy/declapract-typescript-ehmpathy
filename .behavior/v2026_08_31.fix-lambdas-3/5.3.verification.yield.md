# 5.3 verification — the buttonup gate

ground run **2026-09-14**. every claim below cites the exact command + observed result. no
`npx declapract plan` was run (outside the allowlist); the deliverable is the practice LIBRARY, so
its own tests (unit + integration) are the proof — the declapract pipeline is exercised end-to-end
by the `*.declapract.integration.test.ts` clamps that run `executeApply` against temp-repo fixtures.

## tests executed — with proof

| suite | command | result | proof |
|---|---|---|---|
| types | `rhx git.repo.test --what types --mode apply` | ✓ | exit 0, no errors |
| lint | `rhx git.repo.test --what lint --mode apply` | ✓ | exit 0 (after the one repair below) |
| format | `rhx git.repo.test --what format --mode apply` | ✓ | exit 0, no errors |
| unit | `rhx git.repo.test --what unit --mode apply --thorough` | ✓ | exit 0, 826 passed / 0 failed / 0 skipped, 107 suites |
| integration | `rhx git.repo.test --what integration --mode apply --thorough` | ✓ | exit 0, 399 passed / 0 failed / 0 skipped, 39 suites |
| acceptance | — | n/a | this repo is a practice LIBRARY, not a deployed service; no `*.acceptance.test.ts` exists (glob → 0) |

## the one defect this gate found + FIXED

- **lint — import-sort in the new bad-practice declaration.**
  `src/practices/cicd-service/bad-practices/rds-jobs-in-non-rds-service/.github/workflows/provision.yml.declapract.ts`
  carried an unsorted import group (biome `assist/source/organizeImports`). REPAIRED at source to
  biome's canonical order (declapract type import, blank, node builtins grouped with the local
  import). re-run: lint exit 0. this was a defect of a file authored earlier this behavior, caught
  and closed here per the buttonup mandate (no deferral).

## zero skips verified

- `.skip(` / `.only(` across `src/**/*.test.ts` → **0 matches** (Grep)
- `describe|it|test|given|when|then` `.skip`/`.only` → **0 matches**
- silent credential bypass (`if (!<cred|key|token|env>) return`) in test files → **0 matches**
- no prior failures carried forward — every suite is green from a fresh `--thorough` run

## snapshot coverage + change rationalization

the contract outputs this behavior ships are CI workflow templates + declared file emissions; each
is snapshotted by its `*.declapract.test.ts` / `.integration.test.ts` clamp and asserted green in the
826+399 above. snapshot moves this session:

| snap | change | intended? | rationale |
|---|---|---|---|
| `divergentStageGuard...snap` | moved `src/__snapshots__/` → `src/practices/serverless/__snapshots__/` | yes | rode the r11#2 guard relocation to the serverless practice root; content byte-identical, 19/2 green from new home |
| `acceptanceContempFleetGuard...snap` | moved `src/__snapshots__/` → `src/practices/cicd-service/__snapshots__/` | yes | rode the r11#2 guard relocation to the cicd-service practice root; content byte-identical |

no snapshot CONTENT changed in this verification round; the two moves are pure relocations whose
suites pass from their new locations.

## behavior coverage — vision critipaths → clamp

| vision case | clamp / artifact | status |
|---|---|---|
| case=1 upgrade-into-one-axis | serverless template + `stageAccessBridge` (agree today) | ✓ green |
| case=2 cross-service-never-404s | dual-publish `accessByStage` fan-out, `divergentStageGuard` | ✓ green |
| case=3 self-dual-publish-then-flip | deploy-sls dual-fleet publish clamp | ✓ green |
| case=4 value-rewrite-refuses-to-guess | `asPackageJSON` ratchet, `old-stage-enum` review-marker | ✓ green |
| case=5 fix-cannot-create-a-violation | serverless-yml convergence round-trip clamp | ✓ green |
| case=6 the-silent-check-speaks | testdb-preflight-failloud, 586/589/595 receipts | ✓ green |
| case=7 stateful-store-alias-not-copy | `empty-template-declares-zero-clusters` clamp (wish declares ZERO clusters) | ✓ green |
| case=8 artifact-runs-on-its-deploy-node | follow-on (deploy-runtime cluster), demoed not shipped | itemized follow-on |

## blockers

- none.
