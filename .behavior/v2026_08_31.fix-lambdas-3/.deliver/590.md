delivered on branch vlad/fix-lambdas-3.

1. floor bump — `tests-any/best-practice/package.json` test-fns minVersion `1.15.8` → `1.17.0`.

2. wired BOTH autoprune hooks into all 6 shipped jest config templates (the per-runner-config cost the ticket names):
   - tests-node: jest.unit / jest.integration / jest.acceptance .config.ts
   - tests-expo: jest.unit / jest.integration / jest.acceptance .config.ts
   each gets `globalSetup: 'test-fns/autoprune.setup.jest'` + `globalTeardown: 'test-fns/autoprune.teardown.jest'`. the unit config carries the full rationale; the other five carry a one-line note + a pointer to the clamp (rule.avoid.hazard-pitch-prose — no 6x essay). vitest is NOT wired: all 6 shipped configs are jest; no vitest config templates exist in these practices.

3. clamp — `tests-any/autoprune-hooks.declapract.test.ts` (18 tests = 6 configs x 3). asserts globalSetup present, globalTeardown present, and both-present-together (the half-wired hazard: globalSetup with no globalTeardown stamps every dir and reclaims none). teeth confirmed: a drop of globalTeardown from one config reddens 2 (teardown-present + both-together). the clamp reads the template as TEXT, so it is independent of the test-fns version installed HERE — the autoprune subpaths only need to load in a CONSUMER, which the floor bump guarantees. GREEN with the hooks in place; typecheck clean.

## honest bounds / not-done

- **installed test-fns here is 1.15.7** (older than even the prior floor). its exports map has no `autoprune.setup.jest`/`autoprune.teardown.jest`. the exact subpath strings come from test-fns's OWN runtime notice (quoted verbatim in the ticket), which is authoritative. the best-practice configs are TEMPLATES (not executed in this repo), so the hooks do not break this repo's own test run; a consumer that installs >=1.17.0 via the floor gets the live modules.
- **self-adoption not done (flagged):** this repo consumes its own practices, but its OWN root jest configs + its OWN package.json test-fns pin were NOT bumped/wired. that step needs a real `npm install` of 1.17.0 (offline-unverifiable here) and would break this repo's own run until the modules are present. it is a #477-style self-application step, out of this ticket's scope (which names the 6 template sites). flagged for a follow-on.
- **preset (test-fns/jest.preset):** open upstream (ehmpathy/test-fns, vision register row 13) — the need is settled, the merge shape is not. not adopted here; 6 configs still share no base.
- **the second, unrelated drift** (test-fns's own `.publish-npm.yml` carried `npm@latest`, broke a release on EBADENGINE) lives in `ehmpathy/test-fns` and is already fixed there — OUT OF SCOPE for this practice-library repo. noted only for traceability; a check for other consumer repos with the stale copy is a separate sweep.
