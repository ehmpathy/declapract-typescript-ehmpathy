delivered on branch vlad/fix-lambdas-3.

deleted the dead hand-written composite `interface Config` twin from BOTH best-practices, so the
grant-aware `z.infer` `Config` in `config.schema.ts` is the sole source.

## what was gone

4 files, deleted (not prepended, not overwritten):

- `src/practices/config/best-practice/src/utils/config/Config.ts` + its `.declapract.ts`
- `src/practices/persist-with-rds/best-practice/src/utils/config/Config.ts` + its `.declapract.ts`

the `config` practice opened the hand-written `interface Config`; `persist-with-rds` injected a
`database: {...}` block into it via a string-replace declaration. the pair had NO reader —
`getConfig` imports `Config` from `./config.schema`, not from `./Config`. a grep of both
best-practice trees for `from './Config'` returns zero. so the twin could only drift from the live
schema, never constrain it — and it DID drift in a stamped repo (#588): its `access` union read
`'test' | 'dev' | 'prod'` (`dev` is a stage value, never an access tier; `prep` absent), read as a
sanctioned contract and traced to a prod defect.

## clamp

`src/practices/config/no-hand-written-config-type.declapract.test.ts` (5 tests) — at the practice
ROOT (a `.declapract.test.ts` under best-practice/ would be copied to a consumer). asserts: both
best-practice `Config.ts` files absent, both `.declapract.ts` absent, and `config.schema.ts` exports
`export type Config<TGrant extends Grant`. teeth confirmed: re-create either `Config.ts` and its case
reddens (measured 1 red). GREEN with the twin gone; typecheck clean.

## bounds / not-done

- **the bad-practice sweep (wish item 4) is a FOLLOW-ON, deliberately.** a blind bad-practice that
  deletes a consumer's `Config.ts` on `declapract fix` would break a consumer that still imports
  `./Config` — the exact caller-without-callee hazard this wish's reconciliation guard is meant to
  catch. so this delivery removes the twin from the LIBRARY (no consumer inherits it on a fresh
  stamp) but does NOT ship an active removal against an extant consumer. that step needs the
  caller↔callee reconciliation guard first, and is flagged for it.
