wish =

test-fns's latest version has a slowtest reporter for jest

rhachet-roles-ehmpathy has recent upgraded to use it, so you can browse the repo via git.repo.get to see how 

we want to make that a best practice, so that all tests get a slowtest reporter by default

we also want to make sure that the slowtest output file is added to .gitignore, just like it was in rhachet-roles-ehmpathy

