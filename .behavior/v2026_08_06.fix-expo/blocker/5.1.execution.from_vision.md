# blocker — 5.1.execution.from_vision

the wish is DELIVERED end-to-end. the reviewer-infra wall that gated i005–i012 is REPAIRED: the l3
reviewers now return readable tallies. this cycle (i014) is a real convergence, not a wall. both l3
reviewers reached EARNED exhaustion after genuine convergence, so the guard now asks a human to
approve-as-is or extend-budget-and-rerun. full detail: the yield's "i014 convergence" section.

## the current terminal state 🪘

every review level is terminal:

- **l1 (r1–r9): all approved**, 0 blockers / 0 nitpicks each.
- **l3 (r10 `enroll-impl-behavior-intent`, r11 `enroll-impl-arch-defects`): both `exhausted 🌙`,
  terminal.** their budgets (3/3) are fully spent, so a re-arrival cannot re-run them. the i014 tally
  they last emitted: r10 = 2 blockers / 2 nitpicks, r11 = 1 blocker / 3 nitpicks.

per `rule.always.converge-to-terminal`, all levels terminal is the one point at which the human is
the final backstop. the guard states the two human-only options:

- approve as-is: `rhx route.stone.set --stone 5.1.execution.from_vision --as approved`
- extend budget, then rerun: `rhx route.guard.budget --for review --add 1 --stone 5.1.execution.from_vision`

## why this is EARNED exhaustion, not a coast 🛠️

both l3 reviewers' i014 findings converge on ONE headline blocker plus doc nitpicks. every one is
addressed to the reviewers' OWN stated accepted minimum, with a red-on-defect clamp or a cited hold:

| review item | severity | action | verdict |
|-------------|----------|--------|---------|
| **useCases 17-practice duplication, no drift guard** (r10 #1, r11 #1) | 🔴 blocker | added `app-react-native-expo — platform-baseline drift guard` to `cicd-app-react-native-expo/.declapract.integration.test.ts` — reads both usecases from `src/useCases.yml`, asserts every `typescript-project` practice is in expo OR on `expoDivergesFromBaseline`, so a new baseline practice fails CI. this IS the "at minimum, a unit test that asserts the intersection" both reviewers named as the acceptable deferred fix | FIXED to accepted minimum |
| **`domain` EXISTS-unmet path untested** (r10 #3) | 🔴 blocker | added `[t5d]` + `domain` to the migration fixture usecase (+ per-practice symlink): asserts the app-protools-shaped fixture has no `domain/objects/index.ts`, it stays absent after apply (declapract auto-creates no file for a contentless EXISTS best-practice), AND the apply did NOT halt — deps merged, `logic/` move fired | FIXED with clamp |
| two-pass "one fix" gap undocumented (r10 #2) | 🟠 nitpick | `## two-pass migration` sections added to both `old-import-paths` + `relative-imports` `.declapract.readme.md` | DONE |
| `relative-imports` ↔ `directory-structure-src` dependency undocumented (r10 #4, r11 #2) | 🟠 nitpick | the same two readme sections name the pair dependency + the shared `isDeferredToDeprecatedDirMove` guard | DONE |
| `.npmrc` pnpm config under `node`, not `pnpm` (r11 #3) | 🟡 nitpick | created `pnpm/best-practice/.declapract.readme.md` that cross-references `node/.npmrc.declapract.ts`, `defineExpectedNpmrcContents.ts`, `howto.pnpm-expo-hoisted-linker.md` | DONE |
| `terraform-common` contradicts declastruct-goforward (r11 #4) | 🟡 nitpick | HELD-WITH-REASON + flagged as a clean rework for the wisher (see below) | flagged |

the two `.taken.by_self` articulations (i014 r010 + r011) record this convergence with citations, so
a budget extension re-runs the reviewers against the fixed tree + the articulations.

## the two wisher-scope holds (clean reworks, flagged not taken) 🙏

per `rule.always.defer-fulcrums-to-last`, two items are best-guessed and flagged rather than taken,
because each mutates the wisher-settled `useCases.yml` structure:

1. **the `typescript-project-core` extraction** (r11's preferred fix for the headline blocker). the
   ideal decompose-for-recompose shape, precedent-backed (`lambda-service-with-rds`'s double-extends +
   `declapract#17`). NOT taken because it restructures 5+ wisher-settled usecases into a new `extends`
   topology — above this stone's blueprint authority. the drift guard holds the line meanwhile; the
   extraction can land later with zero silent-drift risk. both reviewers accept the drift-guard test
   as the deferred alternative.
2. **drop `terraform-common` from the expo usecase** (r11 #4). the drop is genuinely clean (one line
   in `src/useCases.yml` + one divergence-list entry), and `terraform-common` runs against
   `define.declastruct-goforward.md`. NOT taken because the practice list is wisher-settled; the
   CONTAINS check is inert on a repo with no `provision/` dir, so keep-with-reason carries zero live
   risk. best-guess KEEP, flag the drop for the wisher's council.

## what a human decides 🙏

the stone is content-complete: every blocker addressed to the reviewers' accepted minimum, three
nitpicks closed, one nitpick + one blocker-alternative flagged as clean reworks. the l3 budgets are
spent, so only a human can now either:

- **approve as-is** — every blocker is met to the reviewers' own stated minimum, OR
- **extend budget +1 and rerun** — the higher-quality path: the reviewers re-read the fixed tree +
  the `.taken` articulations and can drop the blockers for a clean verdict (the fixes landed AFTER
  they exhausted, so they have not yet re-verified convergence).

state: full unit suite green (525/525); full integration green (126/126) incl. the 31-test expo
acceptance pipeline with the new drift guard + `[t5d]`; types + format + lint green.
