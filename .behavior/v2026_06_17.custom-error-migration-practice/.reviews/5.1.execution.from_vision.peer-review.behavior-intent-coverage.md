🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-18T11-15-25-972Z
   ├─ review: .behavior/v2026_06_17.custom-error-migration-practice/.reviews/5.1.execution.from_vision.peer-review.behavior-intent-coverage.md
   └─ summary
      ├─ 4 blockers 🔴
      └─ 1 nitpicks 🟠

---
# blocker.1: Behavior intent not fully covered: local path restriction

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.require.behavior-intent-coverage.md

**locations**:
- src/practices/errors/bad-practices/local-error-imports/src/**/*.ts.declapract.ts:35

The wish requires detecting 'imports from local error paths (e.g., @src/utils/errors/*, ../../utils/errors/*)'. The implementation regex matches ANY path ending in /KnownClass (including external packages like 'some-pkg/utils/errors/UnexpectedCodePathError'), which can cause incorrect rewrites of non-local imports. This does not fulfill the specified local-only intent.

**snippet**:
```ts
const FROM_LOCAL_ERROR = `from ['"][^'"]*\/(${KNOWN_CLASSES.join('|')})['"]`;
```

---

# blocker.2: Behavior intent not fully covered: barrel re-exports from index

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.require.behavior-intent-coverage.md

**locations**:
- src/practices/errors/bad-practices/local-error-imports/src/**/*.ts.declapract.ts:35

The vision explicitly lists the edgecase 'import from `utils/errors/index.ts` | matches utils/errors pattern, fixes it'. The regex requires the known class name at the end of the path (/Class['"]), so imports from index.ts or barrel files will not match the pattern, will not be detected by check, and will not be fixed. This requirement from the vision is unaddressed.

**snippet**:
```ts
const FROM_LOCAL_ERROR = `from ['"][^'"]*\/(${KNOWN_CLASSES.join('|')})['"]`;
const LOCAL_ERROR_IMPORT_PATTERN = new RegExp(LOCAL_ERROR_IMPORT_PATTERN_SOURCE, 'm');
```

---

# blocker.3: Behavior intent not fully covered: imported name must match class

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.require.behavior-intent-coverage.md

**locations**:
- src/practices/errors/bad-practices/local-error-imports/src/**/*.ts.declapract.ts:35

The vision requires only rewriting 'imports of common error classes' (name in {} must match the class in path). The regex matches any import { Anything } from '.../KnownClass', and fix will rewrite it (e.g. import { Foo } from 'local/UnexpectedCodePathError' becomes import { Foo } from 'helpful-errors'). This produces incorrect code for mismatched names and is untested/unaddressed.

**snippet**:
```ts
const IMPORT_PREFIX = `(^\\s*import\\s+(?:type\\s+)?\\{[^}]+\\}\\s+)`;
const FROM_LOCAL_ERROR = `from ['"][^'"]*\/(${KNOWN_CLASSES.join('|')})['"]`;
```

---

# blocker.4: Untested code paths from vision

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.require.behavior-intent-coverage.md

**locations**:
- src/practices/errors/bad-practices/local-error-imports/src/**/*.ts.declapract.test.ts:70
- src/practices/errors/bad-practices/local-error-imports/src/**/*.ts.declapract.test.ts:90

Vision specifies handling for 'aliased imports (`import E from ...`)' and 'multi-line imports'. No tests cover import { X as E } or multi-line import statements (even though regex is intended to support them). Per the rule, untested code paths for requirements are blockers.

**snippet**:
```ts
it('should replace multiple local error imports', async () => {
  const contents = `import { UnexpectedCodePathError } from '../../utils/errors/UnexpectedCodePathError';
...
```

---

# nitpick.1: Minor coverage gap: default import syntax

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.require.behavior-intent-coverage.md

**locations**:
- src/practices/errors/bad-practices/local-error-imports/src/**/*.ts.declapract.ts:35

Vision table mentions 'aliased imports (`import E from ...`)'. Current regex only supports named import syntax with {}. While likely not used for these classes, this variant from vision is not explicitly tested or documented in code.

**snippet**:
```ts
const IMPORT_PREFIX = `(^\\s*import\\s+(?:type\\s+)?\\{[^}]+\\}\\s+)`;
```
