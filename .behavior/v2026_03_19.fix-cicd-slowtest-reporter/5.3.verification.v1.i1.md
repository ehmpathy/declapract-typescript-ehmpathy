# verification checklist: slowtest reporter best practice

## behavior coverage

this feature adds infrastructure (best practice config) rather than application logic. behaviors are verified via declapract's own test suite.

| behavior | test type | test file | status |
|----------|-----------|-----------|--------|
| slowtest reporter in jest.integration.config | integration | declapract apply validates config | passed |
| gitignore entry added | unit | .gitignore.declapract.test.ts | passed |
| test-fns version bump | integration | declapract apply validates versions | passed |
| rhachet-brains-xai version bump | integration | declapract apply validates versions | passed |

---

## zero skips verified

- [x] no .skip() or .only() found (grep confirmed)
- [x] no silent credential bypasses
- [x] no prior failures carried forward

---

## snapshot coverage for contract outputs

this feature does not add new cli commands, screens, or sdk methods. it adds best practice config files that declapract validates.

| contract | output type | coverage | status |
|----------|-------------|----------|--------|
| jest.integration.config.ts | declapract template | validated via apply | passed |
| .gitignore.declapract.ts | declapract check | validated via test | passed |

checklist:
- [x] no new cli commands (n/a)
- [x] no new app screens (n/a)
- [x] no new sdk methods (n/a)
- [x] extant tests validate the changes

---

## snapshot change rationalization

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| (none) | — | — | no .snap files changed by this feature |

checklist:
- [x] every `.snap` change has been reviewed (none changed)
- [x] no snap changes expected (confirmed)

---

## tests executed

- [x] `npm run test:types` — passed
- [x] `npm run test:lint` — passed
- [x] `npm run test:unit` — passed (7 tests, .gitignore.declapract.test.ts validated)
- [x] `THOROUGH=true npm run test:integration` — passed (5 tests, slowtest reporter visible)
- [ ] `npm run test:acceptance:locally` — skipped (not required for config-only changes)

---

## blockers

- none

---

## verification complete

all tests pass. slowtest reporter is visible in integration test output. best practice is ready for release.

