# 5.3 verification — fix: mask-timestamps husky guard

## verification checklist

### behavior coverage

the deliverable is a **git pre-commit shell guard** (`check.timestamps.sh`). verification is **runtime behavioral proof** of the hook, via a real automated integration test that builds a temp git repo, stages fixtures, spawns the actual guard, and asserts exit codes.

test file: `src/practices/husky/check.timestamps.declapract.integration.test.ts` (at the practice root, NOT in `best-practice/`, so it is not distributed to consumers).

| behavior (from wish/vision) | test case | status |
|-----------------------------|-----------|--------|
| blocks non-`.ts`/`.sh` file with iso timestamp (exit 2) | case1 | ✅ proven |
| prints verbatim constraint message + names file | case1 | ✅ proven |
| bare time (no lead `T`) matches (optional-T decision) | case2 | ✅ proven |
| `.ts` file exempt (exit 0) | case3 | ✅ proven |
| `.sh` file exempt (exit 0) | case4 | ✅ proven |
| clean file allowed (exit 0) | case5 | ✅ proven |
| date-only does NOT match (no false positive) | case6 | ✅ proven |
| tz-offset form matches (exit 2) | case7 | ✅ proven |

also: regex pre-verified via `rhx grepsafe` against a 10-case fixture (all 5 timestamp variants matched, all 5 non-timestamps rejected).

### zero skips verified
- [x] no `.skip()` / `.only()` in the integration test
- [x] no silent credential bypasses — guard needs no credentials; test needs none
- [x] no prior failures carried forward

### tests executed — with proof

| suite | command | result | proof |
|-------|---------|--------|-------|
| format | `rhx git.repo.test --what format` | ✅ | exit 0, passed |
| lint | `rhx git.repo.test --what lint` | ✅ | exit 0, passed |
| types | `rhx git.repo.test --what types` | ✅ | exit 0, passed |
| integration (guard) | `rhx git.repo.test --what integration --scope check.timestamps --mode apply` | ✅ | exit 0, **9 passed, 0 failed, 0 skipped** |
| integration (full) | `rhx git.repo.test --what integration --mode apply` | ✅ | exit 0, **17 passed, 0 failed, 0 skipped** (3 suites) |

### snapshot coverage
- n/a — the deliverable is a shell pre-commit guard, not a cli/api/sdk/app contract with stdout/screens/responses. its behavior is proven by exit-code + message assertions in the integration test (cases 1-7), not by `.snap` artifacts. no `.snap` files added or changed.

### blockers
- none

## summary

the timestamp guard is fully verified:
- **9/9 dedicated integration tests pass** — real temp-repo + spawned-guard proof of every behavior (block/allow/exempt/false-positive-avoidance across iso, bare, tz-offset, date-only).
- **full integration suite green** (17 passed, 0 failed, 0 skipped).
- **format + lint + types** all exit 0 — no regression.
- the earlier sandbox wall (no direct `sh`, plus git-add/commit blocked) was overcome by the proper mechanism: an automated integration test run via `rhx git.repo.test`, which is both more rigorous and repeatable than a one-off manual shell run.

### note for the wisher (open q2, unchanged)
with the `.behavior/` exception removed per peer review, the root-`.husky/` guard self-blocks this repo's own timestamped route docs. a commit of this PR will need an escape hatch (q2), `--no-verify`, or to not wire the guard into this repo's own root `.husky/`. this is a wisher decision, separate from the guard's verified correctness.
