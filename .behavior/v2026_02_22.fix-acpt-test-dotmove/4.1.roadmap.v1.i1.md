# roadmap: fix acceptance test dotmove

## overview

execute the blueprint to fix hidden directory file loss in `old-acceptance-dir-location` bad practice, then scan the repo for other practices with the same issue.

---

## phase 0: reproduce the bug

**goal**: write an integration test that demonstrates files in hidden directories are not moved

**prereqs**: none

**briefs to read**: none required (standard test patterns)

### checklist

- [ ] **0.1** create integration test file
  - file: `src/practices/tests/bad-practices/old-acceptance-dir-location/.integration.test.ts`
  - use `fs.mkdtemp` to create temp directory
  - create `accept.blackbox/.test/assets/.rhachet/manifest.json`
  - create `accept.blackbox/.test/assets/.agent/config.yml`
  - create `accept.blackbox/somefile.ts`

- [ ] **0.2** write test that applies declapract fix
  - use `executeApply` from declapract sdk
  - assert `blackbox/somefile.ts` exists (should pass)
  - assert `blackbox/.test/assets/.rhachet/manifest.json` exists (should fail — this is the bug)
  - assert `blackbox/.test/assets/.agent/config.yml` exists (should fail — this is the bug)

- [ ] **0.3** run integration test, confirm it fails
  - command: `npm run test:integration -- .integration.test.ts`
  - **acceptance**: test fails on hidden directory assertions

**verification**: `npm run test:integration -- old-acceptance-dir-location` shows failure for hidden path assertions

---

## phase 1: bisect root cause

**goal**: determine if the issue is glob pattern definition or declapract glob behavior

**prereqs**: phase 0 complete (bug reproduced)

**briefs to read**: none required

### checklist

- [ ] **1.1** check declapract's glob implementation
  - locate glob usage in `node_modules/declapract/dist/`
  - check if `dot: true` option is available/enabled

- [ ] **1.2** test with catch-all pattern
  - create `accept.blackbox/**/*.declapract.ts` with same fix logic
  - run integration test
  - **acceptance**: if this fixes hidden dirs, issue is pattern-based

- [ ] **1.3** test with `dot: true` (if applicable)
  - if declapract supports glob options, try to enable `dot: true`
  - **acceptance**: document which approach fixes the issue

**verification**: root cause identified and documented

---

## phase 2: implement fix

**goal**: add catch-all glob pattern to handle all file types in hidden directories

**prereqs**: phase 1 complete (root cause known)

**briefs to read**: `.behavior/v2026_02_22.fix-acpt-test-dotmove/3.3.blueprint.v1.i1.md` (contracts section)

### checklist

- [ ] **2.1** create catch-all declapract file
  - file: `src/practices/tests/bad-practices/old-acceptance-dir-location/accept.blackbox/**/*.declapract.ts`
  - export `check = FileCheckType.EXISTS`
  - export `fix` function that moves to `blackbox/`

- [ ] **2.2** create unit test for catch-all
  - file: `src/practices/tests/bad-practices/old-acceptance-dir-location/accept.blackbox/**/*.declapract.test.ts`
  - test that fix moves `.json` files correctly
  - test that fix moves `.yml` files correctly
  - test that fix moves files in hidden directories

- [ ] **2.3** run unit tests
  - command: `npm run test:unit -- old-acceptance-dir-location`
  - **acceptance**: all unit tests pass

**verification**: `npm run test:unit -- old-acceptance-dir-location` passes

---

## phase 3: verify fix

**goal**: confirm integration test now passes

**prereqs**: phase 2 complete (fix implemented)

**briefs to read**: none required

### checklist

- [ ] **3.1** run integration test
  - command: `npm run test:integration -- .integration.test.ts`
  - **acceptance**: all assertions pass, with hidden directory files

- [ ] **3.2** verify no regressions
  - command: `npm run test:unit`
  - **acceptance**: all unit tests pass

- [ ] **3.3** verify build succeeds
  - command: `npm run build`
  - **acceptance**: build completes without errors

**verification**: `npm run test:integration -- old-acceptance-dir-location` passes

---

## phase 4: apply same fix to `acceptance/` patterns

**goal**: ensure `acceptance/` directory patterns also handle hidden paths

**prereqs**: phase 3 complete (fix verified for `accept.blackbox/`)

**briefs to read**: none required

### checklist

- [ ] **4.1** check if `acceptance/` has same issue
  - review `src/practices/tests/bad-practices/old-acceptance-dir-location/acceptance/` patterns
  - if only `.ts` patterns exist, same bug applies

- [ ] **4.2** add catch-all for `acceptance/` if needed
  - file: `src/practices/tests/bad-practices/old-acceptance-dir-location/acceptance/**/*.declapract.ts`
  - same fix logic as `accept.blackbox/` version

- [ ] **4.3** add unit test for `acceptance/` catch-all
  - file: `src/practices/tests/bad-practices/old-acceptance-dir-location/acceptance/**/*.declapract.test.ts`

- [ ] **4.4** run tests
  - command: `npm run test:unit -- old-acceptance-dir-location`
  - **acceptance**: all tests pass

**verification**: both `accept.blackbox/` and `acceptance/` patterns handle hidden paths

---

## phase 5: document lesson learned

**goal**: create rule brief to crystallize the lesson — then use it to audit the rest of the repo

**prereqs**: phase 4 complete (primary fix done and verified)

**briefs to read**: `.behavior/v2026_02_22.fix-acpt-test-dotmove/3.3.blueprint.v1.i1.md` (rule brief contract)

### checklist

- [ ] **5.1** create rule brief
  - file: `.agent/repo=.this/role=any/briefs/rule.require.glob-includes-dotpaths.md`
  - document the issue: glob patterns may exclude hidden paths
  - document the solution: use catch-all patterns or verify `dot: true`
  - include examples of good vs bad patterns

- [ ] **5.2** update bad practice readme
  - file: `src/practices/tests/bad-practices/old-acceptance-dir-location/.declapract.readme.md`
  - add note about hidden path support

**verification**: rule brief exists and is discoverable

---

## phase 6: scan repo for other violations

**goal**: apply the rule brief to find all other practices with the same issue

**prereqs**: phase 5 complete (rule brief crystallized)

**briefs to read**: `.agent/repo=.this/role=any/briefs/rule.require.glob-includes-dotpaths.md`

### checklist

- [ ] **6.1** enumerate all bad-practice directories
  - command: `find src/practices -type d -name 'bad-practices'`
  - list all bad practices that move/delete files

- [ ] **6.2** identify extension-specific patterns
  - search for `**/*.ts.declapract.ts` patterns
  - search for `**/*.js.declapract.ts` patterns
  - search for other extension-specific patterns
  - **acceptance**: list of all practices with potential hidden path issues

- [ ] **6.3** audit each practice against the rule brief
  - for each practice found, check if it needs to handle hidden paths
  - document which practices need catch-all patterns

**verification**: list of all practices that need fixes

---

## phase 7: fix other violations

**goal**: apply catch-all patterns to all identified practices

**prereqs**: phase 6 complete (violations identified)

**briefs to read**: `.agent/repo=.this/role=any/briefs/rule.require.glob-includes-dotpaths.md`

### checklist

- [ ] **7.1** for each practice with violation:
  - add catch-all `**/*.declapract.ts` pattern
  - add unit test for catch-all
  - verify prior tests still pass

- [ ] **7.2** run full test suite
  - command: `npm run test:unit`
  - **acceptance**: all tests pass

**verification**: all identified practices now have catch-all patterns

---

## phase 8: final validation

**goal**: ensure all tests pass and build succeeds

**prereqs**: phase 7 complete (documentation done)

**briefs to read**: none required

### checklist

- [ ] **8.1** run full test suite
  - command: `npm run test`
  - **acceptance**: all tests pass

- [ ] **8.2** verify build
  - command: `npm run build`
  - **acceptance**: build succeeds

- [ ] **8.3** verify declapract validate
  - command: `npm run test:validate`
  - **acceptance**: validation passes

**verification**: `npm run test && npm run build && npm run test:validate` all pass

---

## completion criteria

- [ ] integration test proves hidden directory files are moved
- [ ] catch-all pattern handles all file types in `accept.blackbox/`
- [ ] catch-all pattern handles all file types in `acceptance/` (if applicable)
- [ ] all other practices with extension-specific patterns audited
- [ ] all identified violations fixed with catch-all patterns
- [ ] unit tests cover hidden path scenarios
- [ ] rule brief documents lesson learned
- [ ] all tests pass
- [ ] build succeeds
