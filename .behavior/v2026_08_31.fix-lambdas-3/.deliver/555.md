verified SATISFIED on branch vlad/fix-lambdas-3 — no new work needed.

## the ground read — #555 was delivered by #559

⚠️ verified against the current tree: commit `551342d` (#559,
"fix(workflows): pin every third-party action ref to a full-length commit sha") already
SHA-pinned every `uses:` across the cicd-* workflow templates. this ticket's acceptance is
fully met by that commit; no work remains.

## the acceptance, checked item by item

- ✅ **no `uses:` in any `src/practices/cicd-*/best-practice/.github/workflows/**` references a
  tag/branch** — a grep for `uses:.*@v[0-9]+$` across `src/practices/**/*.yml` returns ZERO hits
  in any `best-practice/` template. the only `@v` matches are in
  `cicd-common/.test/assets/repo-with-unpinned-workflow/` — the demo `fixture` the actionPins
  integration test clones, which carries unpinned refs ON PURPOSE (the `fixture` domain term:
  a test input free to hold what a template must never hold).
- ✅ **every pinned ref carries a `# vX.Y.Z` trailing comment** — read directly in `.test.yml`,
  `.declastruct.yml`, `review.yml` (e.g. `amannn/action-semantic-pull-request@0723387… # v5`,
  the ticket's own worst-case example).
- ✅ **this repo's own `.github/workflows/**` is pinned the same way** — a grep for a mutable
  `@v` ref there returns ZERO.
- ✅ **the fixture + snapshot are consistent** — the unpinned refs now live in the demo fixture
  by design; the actionPins clamp asserts the fixture set holds ≥1 unpinned ref (anti-vacuity).
- ✅ **an upgrade against an already-pinned repo produces no pin-line diff** — the un-pin
  regression the ticket names is closed: the templates ARE pinned, so a sync no longer reverts a
  consumer's fix.

## the clamp — already present, dogfood-confirmed GREEN

`src/actionPins.declapract.integration.test.ts` walks every workflow yaml under `src/practices/**`
and demands a pin, EXCLUDES the fixture dirs, and re-asserts (anti-vacuity) that the excluded set
holds at least one unpinned ref. run this session: **25 passed, 0 failed**. so the pin policy is
not merely applied — it is guarded against a future un-pin.

## bounds

- the org-level `sha_pinning_required` and dependabot follow-ons the ticket names are explicitly
  out of this task (tracked in `declastruct-github#66`); no action here.
- no code changed this delivery — this is a verify-then-close: #559 + the actionPins clamp already
  satisfy every acceptance item, confirmed by direct grep + a green clamp run.
