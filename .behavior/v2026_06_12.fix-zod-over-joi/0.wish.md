wish =

we want to make joi a bad practice

and instead replace all joi implementations with zod

---

so, as part of the practice, 

replace joi with zod

and replace joi imports with zod imports

and replace basic joi object schemas with zod object schemas (string, number, etc)

