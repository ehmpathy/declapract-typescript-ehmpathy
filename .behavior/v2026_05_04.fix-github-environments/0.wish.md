wish =

checkout the latest pr on ehmpathy/declastruct-github

it rolled out a way to provision github-environments via declastruct

we'd like to use it to support the same exact usecase as in the vision

production-on-main && production-on-else environments

that'll allow us to 
- release without approval any prs that were merged to main (merge has its own approval gates)
- release withyes approval any prs that arnt merged to main (helps folks preview or hotfix, if approved)

